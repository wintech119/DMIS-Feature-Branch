I’m working on a Disaster Relief Inventory Management System (DRIMS/DMIS) project that already has a Flask app, PostgreSQL database, and security hardening in place.

I have an external Python ETL script that I want to add to this Replit project without breaking anything else.

What I need you to do

Create (or replace) a standalone Python file in the project root called:
import_mlss_warehouse_staging.py

I will paste the full contents of this script after this message.

Do not change the logic of the script.

Do not try to “improve” it or refactor it.

Just place it in the file exactly as given.

This script:

Reads MLSS Excel workbooks (including the newer one with donors and odd UOMs).

Normalises UOMs (e.g. 6*27 → pack, Brunswick → tin, 18x30 → bag, 60ftx40ft → sheet, etc.).

Adds donor information into comments_text (e.g. Donor: GOJ for old data, or Donor: Food for the Poor, Donor: JMMB and Sagicor (JMEA) for new data).

Inserts rows into the existing table public.hadr_aid_movement_staging with the current schema (no schema changes).

Do NOT:

Modify any existing Flask routes, blueprints, or configs.

Modify any database schema or migrations.

Change any security settings (CSP, headers, auth, etc.).

Wire this script into the web UI. It should remain a manual CLI utility.

Ensure the project has the required dependencies if they are not already present:

pandas

openpyxl

psycopg2-binary

If needed, just add them to requirements.txt (without removing anything else).

After creating the file, add a short usage comment at the top of the script showing how to run it, for example:

# Example usages:
#   python import_mlss_warehouse_staging.py "MLSS Warehouse Operations.xlsx" --dry-run
#   python import_mlss_warehouse_staging.py "MLSS Warehouse Operations.xlsx" --sql-output mlss_warehouse_import.sql
#   python import_mlss_warehouse_staging.py "MLSS Warehouse Operations.xlsx" --db-url "postgresql://user:pass@host:port/dbname" --create-by "MLSS_IMPORT"


Once you’re ready, I will paste the complete Python script for you to put into