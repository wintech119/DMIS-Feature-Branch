I have an existing DRIMS application in this Replit.
Do not break or change any existing code, UI, workflow, logic, security fixes, or database schema.

I have now populated the table:

public.hadr_aid_movement_staging (
  staging_id      integer generated by default as identity primary key,
  category_code   varchar(30) not null,
  item_desc       text not null,
  unit_label      varchar(25),
  warehouse_code  varchar(10) not null,
  movement_date   date not null,
  movement_type   char(1) not null,   -- 'R' = Received, 'I' = Issued
  qty             numeric(12,2) not null,
  unit_cost_usd   numeric(12,2),
  total_cost_usd  numeric(14,2),
  source_sheet    varchar(64),
  source_row_nbr  integer,
  source_col_idx  integer,
  comments_text   text,
  create_by_id    varchar(20) not null,
  create_dtime    timestamp(0) without time zone not null default current_timestamp
);


This table now includes MLSS warehouse movements (packages, bulk items, donations, procurement, staff issues) with:

movement_type = 'R' for received

movement_type = 'I' for issued

qty = quantity

warehouse_code = warehouse (e.g. UPC / KW, etc.)

item_desc = item or package description

What I want you to do

Update the existing “Aid Movement Dashboard” page
This is currently available at a route like:
/dashboard/aid-movement
(example dev URL: .../dashboard/aid-movement).

Requirements:

Use hadr_aid_movement_staging as the data source.

For the selected filters, compute and display:

Total Received = sum of qty where movement_type = 'R'

Total Issued = sum of qty where movement_type = 'I'

In Store = Total Received − Total Issued

Grouping should be by item and warehouse as needed.

Keep the existing look and feel of the Aid Movement Dashboard, but ensure that:

The numbers now reflect the data in hadr_aid_movement_staging.

The dashboard can handle hundreds of rows without timing out.

Filters on the main dashboard:

Item (by item_desc – with a text search box or dropdown)

Warehouse (by warehouse_code)

Movement date range: date_from / date_to (filter on movement_date)

Optional: Movement type (R / I / All)

The dashboard should show:

High-level summary cards (Total Received, Total Issued, In Store).

A table that lists, per item and warehouse:

Item description

Warehouse

Total Received

Total Issued

In Store

Important constraints:

Do not change any database schema.

Do not remove or break any existing security Hardening (CSP, auth, etc.).

Keep role-based access: this dashboard must remain accessible only to the existing authorized roles (DG, Deputy DG, Director PEOD, Logistics Manager, etc.).

Update / create the Aid Movement Item sub-dashboard

There should be a sub-dashboard or sub-menu under Aid Movement (for example, /dashboard/aid-movement/items) that allows the user to drill into a single item.

Requirements for the item-level dashboard:

Filter controls:

Item (required – by item_desc)

Warehouse (warehouse_code)

Movement type (Received / Issued / All)

Date from / Date to (movement_date range)

For the selected item:

Show Total Received, Total Issued, and In Store for that item across the selected warehouses and date range.

Show a breakdown table per warehouse:

Warehouse

Total Received

Total Issued

In Store

Re-use the existing UI components/styles used on the current Aid Movement Dashboard for cards, tables, and filters.

This sub-dashboard must also read only from hadr_aid_movement_staging.

Implementation details

Find the existing Flask route(s) and templates for the Aid Movement Dashboard (e.g. in a blueprint like dashboard.py or similar).

Do not remove or rename them; just extend/adjust the queries so they aggregate from hadr_aid_movement_staging.

Use safe parameterized queries (no string concatenation with user input).

If there is already a query for aid movement, update it to:

Use movement_type = 'R' and 'I' appropriately.

Sum by item_desc, warehouse_code, and movement_date based on the filters.

Keep pagination and responsive layout behaviour as-is, just ensure the data now reflects the new staging table.

DO NOT:

Do not change any other dashboards or routes.

Do not add or drop any DB columns or tables.

Do not alter login, roles, CSP, or any security-related middleware.

After you update the code:

I should be able to go to /dashboard/aid-movement in the browser and see totals and tables driven by hadr_aid_movement_staging.

I should be able to use the new item-level sub-dashboard to filter one item by warehouse, movement type, and date range, and see the correct totals (Received, Issued, In Store) for that item.

Please show me the updated Flask route(s) and template(s) you changed so I can review them.